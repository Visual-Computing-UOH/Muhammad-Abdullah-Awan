# Week 1 - Internship Progress
## FlyRank AI Machine Learning Internship

**Assignment:** Setup Phase

**Objective:**
To get familiar with the ML workflow by running a real machine learning 
pipeline on real (anonymized) search data, before learning any theory.

**Work Completed:**

1. Set up personal public GitHub repository from FlyRank's official 
   starter template (notebooks, pipeline scripts, starter dataset).
2. Requested access to FlyRank's larger internship dataset on Hugging 
   Face (for use in later weeks).
3. Ran the first notebook (`01_first_look_and_discovery.ipynb`) in 
   Google Colab — executed a full ML pipeline: cleaned the data, built 
   a hand-written baseline rule, trained three ML models (logistic 
   regression, decision tree, random forest), and evaluated them.
4. Compared baseline rule vs trained model using Precision@50 metric. 
   Learned model scored 0.740 vs baseline's 0.240 — roughly a 3x 
   improvement.
5. Completed second notebook (`02_your_first_readable_model.ipynb`) — 
   experimented with model settings (tree depth, feature selection), 
   validated results using proper train/test split to avoid data leakage.
6. Saved and committed both completed notebooks to GitHub repository, 
   submitted repository link on assignment portal.

**Files in this folder:**
- `01_first_look_and_discovery.ipynb`
- `02_your_first_readable_model.ipynb`

